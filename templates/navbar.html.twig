{% set tab = tab ?? '' %}

{% set login_state = app.session.get('loggedIn') ? 'true' : 'false' %}
{% set first_name = app.session.get('firstName') ?? null %}
{% set last_name = app.session.get('lastName') ?? null %}
{% set member_id = app.session.get('memberId') ?? null %}


<nav class="navbar bg-faded justify-content-between flex-nowrap flex-row navbar-dark bg-dark" {# style="background-color: #90d1ff;" #}>
    <div class="container-fluid"><a class="navbar-brand float-left" href="{{ path('index') }}">Der glühende Colt</a>
        <div class="justify-content-end">
            <ul class="navbar-nav mb-0 mb-lg-0 flex-row float-right">
                {% if login_state == 'true' %}
                    <li class="nav-item d-flex align-items-center"><a class="nav-link" id="user">
                            User: {{ first_name }} {{ last_name }}</a></li>
                    <li class="nav-item d-flex align-items-center ms-2"><a
                                class="nav-link {% if tab == 'members' %} fw-bold {% endif %}"
                                aria-current="page" href="{{ path('members') }}">Mitglieder</a></li>
                    <li class="nav-item d-flex align-items-center ms-2"><a class="nav-link btn btn-outline-primary px-1" href="{{ path('logout') }}">Abmelden</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
