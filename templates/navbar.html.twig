{% set tab = tab ?? '' %}

{% set login_state = app.session.get('loggedIn') ? 'true' : 'false' %}
{% set first_name = app.session.get('firstName') ?? null %}
{% set last_name = app.session.get('lastName') ?? null %}
{% set member_id = app.session.get('memberId') ?? null %}

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">

        <a class="navbar-brand" href="{{ path('index') }}">Der glühende Colt</a>

        {% if login_state == 'true' %}

           <div>
               <p class="nav-link"> User: {{ first_name }} {{ last_name }}</p>
           </div>

        {% endif %}

        <div class="navbar-nav">
        {% if login_state == 'true' %}
            <a class="nav-link {% if tab == 'members' %} fw-bold {% endif %}"
               aria-current="page" href="{{ path('members') }}">Mitglieder</a>
            <a class="nav-link btn btn-outline-dark" href="{{ path('logout') }}">Abmelden</a>
        {% endif %}


        </div>
    </div>
</nav>